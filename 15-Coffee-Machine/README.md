Modular Design: Broke down a complex system into four distinct helper functions to handle resources, coins, transactions, and production.

Resource Tracking: Implemented logic to decrement global ingredients based on nested dictionary data.

Financial Logic: Managed global state for profit and utilized the round() function to handle floating-point arithmetic for currency.

Input Handling: Developed a multi-path control flow allowing for user orders, maintenance reports, and machine shutdown.