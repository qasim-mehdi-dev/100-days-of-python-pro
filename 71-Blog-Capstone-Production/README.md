# Flask Blog Application

A secure, fully-featured Flask blog application complete with user authentication, rich-text editing, and a relational database.

## Local Setup

1. **Install Dependencies:**
   ```bash
   py -m pip install -r requirements.txt